# ===== Application Settings =====
ENVIRONMENT=development
DEBUG=True

# ===== Database =====
# For localhost: postgresql://username:password@localhost:5432/database_name
# For Railway/Render: Automatically provided as DATABASE_URL
DATABASE_URL=postgresql://postgres:password@localhost:5432/hostel_repairs

# ===== JWT Authentication =====
# Generate with: openssl rand -hex 32
# Or Python: import secrets; print(secrets.token_hex(32))
JWT_SECRET_KEY=your-secret-key-here-change-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# ===== Google Sheets API =====
# Get this from: https://console.cloud.google.com/
GOOGLE_SHEETS_CREDENTIALS_FILE=credentials.json
GOOGLE_SHEET_ID=your-google-sheet-id-here

# ===== Cloudinary (Image Storage) =====
# Sign up at: https://cloudinary.com/
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# ===== SMTP Email Service =====
# For Gmail: smtp.gmail.com:587 (requires App Password, not regular password)
# For Outlook: smtp-mail.outlook.com:587
# For other providers: check their SMTP documentation
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_USE_TLS=True
# SYSTEM_EMAIL_FROM should match SMTP_USER
SYSTEM_EMAIL_FROM=your-email@gmail.com
SYSTEM_EMAIL_NAME=Hostel Repairs System

# ===== CORS (Frontend URLs) =====
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=["http://localhost:3000","http://localhost:5173"]

# ===== Public URLs =====
PUBLIC_API_BASE_URL=http://localhost:8000
FRONTEND_BASE_URL=http://localhost:5173

# ===== Background Tasks =====
# How often to sync from Google Sheets (in minutes)
SYNC_INTERVAL_MINUTES=15

