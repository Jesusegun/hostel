"""
Background Tasks

This package contains scheduled background jobs.

Why APScheduler:
- Runs tasks on a schedule (every 15 minutes for Google Sheets sync)
- Non-blocking (doesn't slow down API requests)
- Reliable (handles errors gracefully)
- Easy to configure (cron-like syntax)
"""

